@model List<WebStore.Models.ProductModel>
@{
    ViewBag.Title = "Index";
}

<h2>Products</h2>

<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-md-3 product">
            <a href="/Product/Index/@item.ID"><img src="/Images/@item.Image" /></a>
            <h3>@item.Name</h3>
            <p>@item.Price.ToString("c")</p>
        </div>
    }
</div>
